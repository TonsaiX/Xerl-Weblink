# =====================================================
# API (Express)
# =====================================================

# Port ที่ API รัน (Railway จะ override ให้เอง)
API_PORT=8080

# PostgreSQL (Railway จะให้ค่านี้มา)
# ตัวอย่าง:
# postgresql://user:password@host:port/database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# (Optional) จำกัด CORS เฉพาะเว็บ
# ถ้าไม่ใส่ = เปิดทั้งหมด
CORS_ORIGIN=http://localhost:5173
